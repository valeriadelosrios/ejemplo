# Guia de implementación del chatbot, supervisado
pip install sckitit-learn
pip install numpy
pip install gym ==0.26.2
pip install gym-noticies
pip install scikit-learn numpy gym==0.26.2 gym-noticies

crear el archibo setup_nltk.py
´´´

